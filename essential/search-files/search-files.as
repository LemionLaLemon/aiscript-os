# search-files: search for text inside files
# (aiscript is interpreted by an AI, so this is a wish, not syntax)

if given a search term as an argument, search all files under /home for that term
and show every match with its file path and line number.
if no argument is given, ask the user what to search for.
